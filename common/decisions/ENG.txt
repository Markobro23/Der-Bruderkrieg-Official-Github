ENG_chose_victor_of_elections = {
	ENG_election_timeout = {
	
		priority = 10
	
		icon = generic_ignite_civil_war
	
		available = {
			hidden_trigger = { always = no }
		}
	
		days_mission_timeout = 345
	
		fire_only_once = yes
	
		activation = {
		tag = ENG
		has_completed_focus = ENG_general_elections
		}
	
		visible = {
			
		} 
	
		cancel_if_not_visible = yes
	
		is_good = yes
	 
		timeout_effect = {
			ENG_elections_timeout_effect = yes
		}
	}
	
}

ENG_halifax_war_in_ireland = {
	ENG_ireland_protest_mission = {
	
		priority = 10
	
		icon = generic_ignite_civil_war
	
		available = {
			hidden_trigger = { always = no }
		}
	
		days_mission_timeout = 330
	
		fire_only_once = yes
	
		activation = {
			has_completed_focus = ENG_continue_the_war_in_ireland
		}
	
		visible = {
			NOT = {
				has_completed_focus = ENG_complete_irish_integration
			}
		} 
	
		cancel_if_not_visible = yes
	
		is_good = yes
	 
		timeout_effect = {
			custom_effect_tooltip = USA_the_irish_civil_war_will_begin
			hidden_effect = {
				IRA = {
					transfer_state = 134
					transfer_state = 135
					declare_war_on = {
						target = IRE
						type = civil_war
					}
				}
			}
		}
	}
	ENG_ireland_attack_mission = {
	
		priority = 10
	
		icon = generic_ignite_civil_war
	
		available = {
			hidden_trigger = { always = no }
		}
	
		days_mission_timeout = 25
	
		fire_only_once = yes
	
		activation = {
			has_completed_focus = ENG_continue_the_war_in_ireland
		}
	
		visible = {
			NOT = {
				has_completed_focus = ENG_complete_irish_integration
			}
		} 
	
		cancel_if_not_visible = yes
	
		is_good = yes
	 
		timeout_effect = {
			IRE = {
				random_owned_controlled_state = {
					add_dynamic_modifier = {
						modifier = ira_attack_modifier
					}
				}
			}
		}
	}

	##Crush Riots
	ENG_crush_riots = {

		icon = eng_trade_unions_demand

		allowed = {
		}

	

		days_remove = 0 



		ai_will_do = {
			factor = 100
		}
		on_map_mode = map_and_decisions_view
		highlight_states = {
			state = FROM
		}
		cost = 0

		target_array = IRE.owned_states
		state_target = yes
		fire_only_once = no
		visible = {
			FROM = {
				has_dynamic_modifier = {
					modifier = ira_attack_modifier
				}
			}
			
		}

		available = {
			ENG = {
				has_completed_focus = ENG_crush_the_sein_fienn_rioters
			}
			divisions_in_state = {
				size > 3
				state = FROM
			}
		}
		complete_effect = {
			FROM = {
				remove_dynamic_modifier = {
					modifier = ira_attack_modifier
				}
			}
			ENG = {
				add_to_variable = {
					tooltip = ENG_irish_integration_increase_by_5
					ENG_irish_integration = 5
				}
			}
		}
	}

	## End
	ENG_increase_integration_for_ireland = {

		icon = eng_trade_unions_demand

		allowed = {
		}

	

		days_remove = 50
		days_re_enable = 15 

		cost = 25

		fire_only_once = yes

		ai_will_do = {
			factor = 100
		}
		available = {
			
		}
		visible = {
		}
		remove_effect = {
			add_to_variable = {
				tooltip = ENG_irish_integration_increase_by_5
				ENG_irish_integration = 5
			}
		}
	}
}

ENG_bring_albert_from_his_paranoia = {
	ENG_monthly_meeting_with_albert = {

		icon = generic_civil_support

		cost = 0

		fire_only_once = no

		days_remove = 30

		complete_effect = {
			
			add_to_variable = {
				tooltip = ENG_kings_approval_var_increase_by_5
				ENG_kings_approval_var = 5
			}
		}

	}
	ENG_albert_defender_of_britain = {

		icon = generic_civil_support

		cost = 15

		fire_only_once = yes

		complete_effect = {
			add_popularity = {
				ideology = ROOT
				popularity = 0.025
			}
			add_to_variable = {
				tooltip = ENG_kings_approval_var_increase_by_10
				ENG_kings_approval_var = 10
			}
		}

	}

	ENG_albert_protector_of_windsor = {

		icon = generic_civil_support

		cost = 25

		fire_only_once = yes

		complete_effect = {
			add_to_variable = {
				tooltip = ENG_kings_approval_var_increase_by_10
				ENG_kings_approval_var = 10
			}
			add_war_support = 0.03
		}

	}

	ENG_fight_for_albert = {

		icon = generic_industry

		cost = 0

		fire_only_once = yes

		available = {
			has_war = yes
		}

		complete_effect = {
			add_to_variable = {
				tooltip = ENG_kings_approval_var_increase_by_10
				ENG_kings_approval_var = 10
			}
		}

		days_remove = 100

		modifier = {
			weekly_manpower = 100
			mobilization_speed = 0.1
		}

	}

	ENG_alberts_europakrieg_knowledge = {

		icon = generic_prepare_civil_war

		cost = 50

		fire_only_once = yes

		available = {
			has_war = yes
		}

		complete_effect = {
			add_to_variable = {
				tooltip = ENG_kings_approval_var_increase_by_10
				ENG_kings_approval_var = 10
			}
			army_experience = 5
			add_tech_bonus = {
				name = land_doc_bonus
				bonus = 0.25
				uses = 1
				category = land_doctrine
			}
		}

	}

}

weltkrieg_decisions = {
	ENG_complete_mobilization = {

		priority = 10

		icon = generic_ignite_civil_war

		available = {
			hidden_trigger = { always = no }
		}

		days_mission_timeout = 25

		fire_only_once = yes 

		activation = {
			tag = ENG
			has_country_flag = ENG_activation_complete_mobilization_flag
		}

		visible = {
			has_idea = SOV_the_mobilized_forces
		} 

		cancel_if_not_visible = yes

		is_good = yes

		timeout_effect = {
			remove_ideas = SOV_the_mobilized_forces
			create_wargoal = {
				target = GER
				type = topple_government
			}
			add_war_support = 0.15
		}
	}
	ENG_invade_prussia_for_memes = {

		allowed = {
			tag = ENG
		}

		fire_only_once = yes

		ai_will_do = {
			factor = 1000
		}

		icon = generic_ignite_civil_war

		visible = {
			has_war_with = SOV
			NOT = {
				has_country_flag = UK_chose_mosley
				has_country_flag = UK_chose_attlee
			}
		}

		cost = 0
		days_remove = 35

		available = {
		}

		remove_effect = {
			activate_mission = ENG_complete_mobilization
			set_country_flag = ENG_activation_complete_mobilization_flag
			add_ideas = SOV_the_mobilized_forces

			hidden_effect = {
				AUS = {
					country_event = { id = british_inteverntion_in_prussian_expansion.3 days = 2 }
				}
				GER = {
					country_event = {
						id = british_inteverntion_in_prussian_expansion.8
						days = 3
					}
				}
			}

		}

	}
}

ENG_withdraw_from_hanover_cat = {
	
	ENG_HAN_withdrawal_mission = {

		allowed = {
			OR = {
				tag = HAN
				tag = GER
				tag = ENG
			}
		}

		activation = {
			has_global_flag = UK_is_a_pussy
		}

		available = {
			HAN = {
				has_war_with = GER
			}
		}

		is_good = no
		fire_only_once = yes
		days_mission_timeout = 35

		timeout_effect = {
			effect_tooltip = {
				news_event = ENG_Goering.3
				ENG = {
					diplomatic_relation = {
						country = HAN
						relation = guarantee
						active = no
					}
				}
				GER = {
					add_timed_idea = {
						idea = GER_pact_with_britain
						days = 200
					}
					add_threat = 5
				}
				HAN = {
					remove_ideas = HAN_anglo_expeditionary_force
				}
			}
			if = {
				limit = { tag = GER }
				hidden_effect = {
					news_event = ENG_Goering.3
					add_timed_idea = {
						idea = GER_pact_with_britain
						days = 200
					}
					add_threat = 5
				}

			}
			if = {
				limit = { tag = ENG }
				hidden_effect = {
					diplomatic_relation = {
						country = HAN
						relation = guarantee
						active = no
					}
				}
			}
			if = {
				limit = { tag = HAN }
				hidden_effect = {
					remove_ideas = HAN_anglo_expeditionary_force
				}
			}
		}

	}

	GER_make_threatening_noises = {

		allowed = {
			tag = GER
		}

		fire_only_once = yes

		icon = generic_ignite_civil_war

		visible = {
			has_active_mission = ENG_HAN_withdrawal_mission
			check_variable = { days_mission_timeout@ENG_HAN_withdrawal_mission > 7 }
		}

		cost = 10

		available = {
			has_war_support > 0.69
		}

		complete_effect = {
			every_country = {
				limit = {
					OR = {
						tag = GER
						tag = HAN
						tag = ENG
					}
				}
				country_event = { id = ENG_Goering.10 }
			}
		}

	}

	GER_declare_war_on_HAN_anyways = {

		allowed = {
			tag = GER
		}

		icon = generic_ignite_civil_war

		visible = {
			has_active_mission = ENG_HAN_withdrawal_mission
			check_variable = { days_mission_timeout@ENG_HAN_withdrawal_mission > 4 }
		}

		ai_will_do = {
			factor = 0
		}

		cost = 50

		available = {
			strength_ratio = {
				tag = HAN
				ratio > 2
			}
			custom_trigger_tooltip = {
				tooltip = GER_at_least_3_troops_hanover_border_state
				OR = {
					divisions_in_state = {
						size > 2
						state = 60
					}
					divisions_in_state = {
						size > 2
						state = 64
					}
					divisions_in_state = {
						size > 2
						state = 61
					}
					divisions_in_state = {
						size > 2
						state = 58
					}
				}
			}
		}

		complete_effect = {
			declare_war_on = {
				target = HAN
				type = annex_everything
			}
		}

	}

	ENG_speed_up_withdrawal = {

		allowed = {
			tag = ENG
		}

		fire_only_once = yes

		icon = eng_trade_unions_demand

		visible = {
			has_active_mission = ENG_HAN_withdrawal_mission
			check_variable = { days_mission_timeout@ENG_HAN_withdrawal_mission > 9 }
		}

		cost = 20

		available = {
			has_war_support < 0.6
		}

		complete_effect = {
			add_days_mission_timeout = {
				mission = ENG_HAN_withdrawal_mission
				days = -7
			}
			every_country = {
				limit = {
					OR = {
						tag = GER
						tag = HAN
					}
				}
				country_event = { id = ENG_Goering.9 }
			}
		}

	}

	ENG_slow_down_withdrawal = {

		allowed = {
			tag = ENG
		}

		fire_only_once = yes

		icon = eng_trade_unions_demand

		visible = {
			has_active_mission = ENG_HAN_withdrawal_mission
			check_variable = { days_mission_timeout@ENG_HAN_withdrawal_mission > 3 }
		}

		cost = 20

		available = {
			has_war_support > 0.3
		}

		complete_effect = {
			every_country = {
				limit = {
					OR = {
						tag = GER
						tag = HAN
						tag = ENG
					}
				}
				country_event = { id = ENG_Goering.8 }
			}
		}

	}

	HAN_sabotage_evacuation_route = {

		allowed = {
			tag = HAN
		}

		days_re_enable = 10

		icon = generic_ignite_civil_war

		visible = {
			has_active_mission = ENG_HAN_withdrawal_mission
			check_variable = { days_mission_timeout@ENG_HAN_withdrawal_mission > 15 }
			ENG = {
				NOT = {
					has_country_flag = ENG_done_with_HAN_sht
				}
			}
		}

		cost = 25

		complete_effect = {
			if = {
				limit = { check_variable = { HAN_poking_uk < 2 } }
				every_country = {
					limit = {
						OR = {
							tag = GER
							tag = HAN
							tag = ENG
						}
					}
					country_event = { id = ENG_Goering.8 }
				}
				else = {
					ENG = {
						country_event = ENG_Goering.11
					}
				}
			}
			add_to_variable = { HAN_poking_uk = 1 }
		}

	}

	HAN_support_defection_british_forces = {

		allowed = {
			tag = HAN
		}

		fire_only_once = yes

		icon = eng_trade_unions_demand

		visible = {
			has_active_mission = ENG_HAN_withdrawal_mission
			check_variable = { days_mission_timeout@ENG_HAN_withdrawal_mission > 4 }
			ENG = {
				NOT = {
					has_country_flag = ENG_done_with_HAN_sht
				}
			}
		}

		cost = 50

		complete_effect = {
			division_template = {
				name = "British Expeditionary Force"
				division_names_group = ENG_INF_01
				is_locked = yes
				regiments = {
					infantry = { x = 0 y = 0 }
					infantry = { x = 0 y = 1 }
					infantry = { x = 0 y = 2 }
			        infantry = { x = 1 y = 0 }
					infantry = { x = 1 y = 1 }
					infantry = { x = 1 y = 2 }
			        infantry = { x = 2 y = 0 }
					infantry = { x = 2 y = 1 }
					infantry = { x = 2 y = 2 }
				}
			}
			capital_scope = {
				create_unit = {
				    division = "name = \"British Expeditionary Force\" division_template = \"British Expeditionary Force\" start_experience_factor = 0.3"
				    owner = HAN
				}
			}
			add_to_variable = { HAN_poking_uk = 1 }
		}

	}

}


ENG_crushing_bose_rebels = {
	ENG_crushing_bose_rebels_in_FROM = {

		icon = eng_trade_unions_demand

		allowed = {
		}

	

		days_remove = 0 



		ai_will_do = {
			factor = 100
		}
		on_map_mode = map_and_decisions_view
		highlight_states = {
			state = FROM
		}
		cost = 0
		target_trigger = {
			FROM = {
				has_dynamic_modifier = {
					modifier = bose_is_annoying_attack_modifier
				}
			}
		}
		state_target = yes
		fire_only_once = no
		visible = {
			FROM = {
				has_dynamic_modifier = {
					modifier = bose_is_annoying_attack_modifier
				}
			}
			
		}

		available = {
			divisions_in_state = {
				size > 3
				state = FROM
			}
		}
		complete_effect = {
			FROM = {
				remove_dynamic_modifier = {
					modifier = bose_is_annoying_attack_modifier
				}
			}
			
		}
	}
}

ENG_imperial_federation_category = {
	ENG_imperial_federation_nationalization_act = {
	
		priority = 10
		icon = decision_revolt
	
		available = {
			hidden_trigger = { always = no }
		}
	
		days_mission_timeout = 1
	
		fire_only_once = yes
	
		activation = {
			has_completed_focus = ENG_national_centralization_act
		}
	
		visible = {
			tag = ENG
		} 
	
		cancel_if_not_visible = yes
	
		is_good = yes
	 
		timeout_effect = {
			AST = {
				country_event = ENG_imperial_federation_tree.1
			}
			SAF = {
				country_event = ENG_imperial_federation_tree.1
			}
			CAN = {
				country_event = ENG_imperial_federation_tree.1
			}
		}
	}

	ENG_imperial_federation_centralization_act = {
	
		priority = 10
		icon = decision_revolt
	
		available = {
			hidden_trigger = { always = no }
		}
	
		days_mission_timeout = 1
	
		fire_only_once = yes
	
		activation = {
			has_completed_focus = ENG_imperial_centralization_act
		}
	
		visible = {
			tag = ENG
		} 
	
		cancel_if_not_visible = yes
	
		is_good = yes
	 
		timeout_effect = {
		
			SAF = {
				country_event = ENG_imperial_federation_tree.1
			}
			CAN = {
				country_event = ENG_imperial_federation_tree.1
			}
		}
	}
}